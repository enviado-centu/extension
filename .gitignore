# Paquetes que genera el navegador al empaquetar la extensión
*.crx
*.pem
*.zip

# Sistema operativo y editores
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/

# Capturas y perfil temporal de la prueba en navegador (tests/e2e/navegador.mjs)
tests/e2e/salida/

# Node (no usamos dependencias, pero por las dudas)
node_modules/
